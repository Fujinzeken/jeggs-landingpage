import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Sojeggs Music | Online Piano & Music Production Training",
  description:
    "Learn piano and music production from beginner to professional level. Master your first song within 30 days using simple practical techniques by Jegede Samuel Olayinka.",
  keywords: [
    "online piano lessons",
    "music production training",
    "learn piano in 30 days",
    "piano instructor nigeria",
    "audio production course",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
